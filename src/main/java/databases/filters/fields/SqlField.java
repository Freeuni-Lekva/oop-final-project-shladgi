package databases.filters.fields;

public interface SqlField{
    public String getColumnName(); // this is for using enums for filter column names to avoid spelling mistakes and make changes easier
}
